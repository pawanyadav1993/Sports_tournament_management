Team 10- Pawan, Natalie, Avinash, William
In Order to execute MySql queries use below steps in given order;

1)run steps-schema.sql
2)run steps-populate.sql
3)run steps-retrieve.sql **NOTE: While executing retrieve you should execute individual queries rather than the whole script a at time,
 otherwise you won't be able to see all the outputs.